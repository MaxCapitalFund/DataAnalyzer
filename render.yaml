services:
  - type: web
    name: trading-analyzer
    env: python3
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: python3 -m http.server 3000 --directory out
    envVars:
      - key: NODE_ENV
        value: production
